---
name: skill-name-here
description: One or two sentences describing what this skill does and, critically, WHEN it should trigger: the phrases, file types, or task shapes that should make Claude reach for it.
---

# Skill Name Here

Brief statement of what this skill helps with.

## When to use this

- Trigger condition 1
- Trigger condition 2

## Steps / Approach

1. ...
2. ...

## Offer to save (optional, only if this skill produces a full report)

If the output is a full report rather than a short verdict, add a final step that follows the save prompt defined in `${CLAUDE_PLUGIN_ROOT}/references/save-states.md`, and give that file its own canonical filename entry in the table there. Skip this step entirely for verdict-style skills, where the output is a short answer about one specific thing rather than a report worth keeping and diffing over time.

## Reference material

Point to anything under `../../references/` or `../../scripts/` this skill relies on, e.g.:
- `${CLAUDE_PLUGIN_ROOT}/references/<file>.md`
- `${CLAUDE_PLUGIN_ROOT}/scripts/<script>.py`
